## Gate Overrides

- [qa-tester-F001] Accepted: the codex reviewer sandbox for this gate run
  cannot read `QA_RULES_DIR` (`/home/screenleon/github/qa-testing-rules`)
  even though it exists on the host — a reviewer-sandbox visibility gap, not
  a missing rules checkout. The three regression suites qa-tester asked to
  be rerun (`tests/shell/test-test-harness.sh`, `tests/shell/test-run-tests.sh`,
  `tests/shell/test-core-schemas.sh`) were run directly by the PM and all
  pass. Tracked for a real fix at [[CC-541]]; do not silently re-accept this
  override on a future PR without re-reading CC-541's status. Owner: user
  (screen.leon@gmail.com), approved 2026-08-04.

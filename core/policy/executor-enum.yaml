# core/policy/executor-enum.yaml
#
# Closed list of executor profiles pm-dispatch dispatches to.
# This enum is embedded inline in core/schema/run.schema.json and
# core/schema/handover.schema.json (kept in sync).
# Intended for consumption by runtime-layer validators (deferred to v0.3.x runtime phase); see core/README.md.
#
# Adding a value is a one-line YAML edit + a breaking version bump per the
# schema versioning policy in core/README.md.

values:
  - codex
  - claude
  - opencode
